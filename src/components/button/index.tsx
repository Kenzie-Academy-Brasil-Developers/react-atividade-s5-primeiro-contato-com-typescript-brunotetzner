//Botão para submeter os dados dos inputs
import { Card } from "../card";
export const Button = () => {
  const changePeoples = () => {
    // setPeoples(...peoples, newPeople);
  };
  return <button onClick={changePeoples}>Add people</button>;
};
